# Copy to .env for local overrides. Vercel: set in Project → Environment Variables.
EXPO_PUBLIC_API_URL=https://tap2crackmaingame-production.up.railway.app

# Local backend only (used when __DEV__ is true and EXPO_PUBLIC_API_URL is unset)
# EXPO_PUBLIC_DEV_API_URL=http://localhost:8000
